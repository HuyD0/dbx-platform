# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage

# Databricks
.databricks/
# Wheel staged into the app source dir by the deploy workflow — never commit
apps/platform-console/wheels/
# Frontend build output (CI builds it; bundle sync re-includes it) — never commit
apps/platform-console/static/
apps/platform-console/frontend/node_modules/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Claude Code — commit shared config (settings.json, hooks, skills); keep personal overrides local
.claude/settings.local.json

# Local env / secrets — never commit credentials
.env
*.pem
